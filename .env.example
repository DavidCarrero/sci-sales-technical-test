# Copy this file to .env and set your own password before running docker compose.
# SQL Server refuses anything under 8 characters or without three of: uppercase,
# lowercase, digits, symbols.
#
#   cp .env.example .env
#
# .env is git-ignored: the real password never reaches the repository.

MSSQL_SA_PASSWORD=ChangeMe_Str0ng!Pass
